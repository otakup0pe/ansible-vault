vault_pre_url: "https://releases.hashicorp.com/vault/{{ vault_version }}/vault_{{ vault_version }}_"
vault_download_url: "{{vault_pre_url}}{{ansible_system|lower}}_{{ansible_machine}}.zip"
vault_download_dest: "{{vault_cache}}/vault-{{vault_version}}.zip"
vault_install_directory: "{{vault_path}}/{{vault_version}}"
vault_binary: "{{vault_install_directory}}/vault"

hcvswitch_repo: "https://github.com/otakup0pe/hcvswitch.git"

vault_safe_url: "https://github.com/starkandwayne/safe/releases/download/v{{vault_safe_version}}/safe-linux-amd64"
